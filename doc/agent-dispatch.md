# Agent dispatch

## Purpose
Runs an agent as a `claude` CLI subprocess, streams its stdout/stderr in near-real-time to the UI, tracks lifecycle (started, exited, killed), and persists a run record for later inspection.

## Key components
- `GigaClaw.Core/Automation/ClaudeRunner.cs` — orchestrates a single agent run. Invokes `claude --print` (no `--remote-control`) and closes stdin after writing the prompt so the subprocess does not block; parallel runs across different worktrees cannot collide via IPC files. Skill file reads do not pass the cancellation token, ensuring a cancelled outer scope cannot leave the run stuck in `Running` state. After each subprocess turn, if `AgentRun.IsAwaitingUserAnswer` is set, the runner first cancels the linked CTS and awaits `PumpSteeringAsync` so that pump task stops consuming from `SteeringQueue` (without this, pump and runner race for the same channel item and the user's answer is silently consumed by the pump, leaving the runner waiting until the 10-minute timeout); remaining queued steer messages are then drained to `PendingSteerMessages` (skipped when `IsAwaitingUserAnswer` — the runner reads the answer itself). The runner then awaits the answer from `SteeringQueue` (10-minute timeout, also cancellable via `AgentRun.Cancellation`); when the answer arrives it is added as a pending steer message and the run continues; a timeout or cancellation clears the flag and resumes without an answer.
- Before memory is appended, the runner reads optional `.agents/contracts.json` and injects only the selected agent's compact contract plus shared defaults. Missing manifests remain backward compatible; malformed manifests fail the dispatch instead of silently omitting guardrails.
- `GigaClaw.Core/Automation/ProcessLifecycleManager.cs` — process spawn, exit, and kill handling.
- `GigaClaw.Core/Automation/ClaudeStreamPump.cs` — pumps NDJSON events from the subprocess into the run's event list. The terminal `result` event's `usage` (input/output/cache token counts) and `total_cost_usd` are parsed and **accumulated** onto the `AgentRun` (`AddUsage`) — accumulated, not assigned, because one run can spawn several subprocesses (resume retry, quota fallback, steer replay), each with its own result event. When the CLI emits `{"type":"result","subtype":"error_max_turns"}`, the pump re-labels the event kind to `"max_turns"` instead of `"result"` (usage is recorded before the re-label). Tool-use events for the `AskUserQuestion` tool are emitted with kind `"ask_user_question"` (instead of `"tool_use"`) so the chat drawer can render an interactive answer widget.
- `GigaClaw.Web/Components/ClaudeChatDrawer.razor` — chat UI component. Handles `max_turns` SSE events by setting `_hitMaxTurns = true` and rendering an inline banner with a **Continue** button; clicking it pre-fills the input with "Continue" and calls `Send()`. Any sent message also clears the banner. Handles `ask_user_question` events by rendering an `AskUserQuestionMessage` widget (multiple-choice buttons or free-text textarea + Submit); the answer is posted to `POST /runs/{runId}/steer` and the widget is then locked as answered. `ParseAskUserQuestionDetail` extracts the prompt and options from the event's JSON input: it first tries the real CLI schema (`questions[0].question` for the prompt text and `questions[0].options[*].label` for choice labels), falling back to the legacy flat schema (`prompt` / `options` as a string array) for backward compatibility with older mocks.
- `GigaClaw.Core/Automation/AgentRun.cs` — in-memory run model + event stream consumed by the UI; carries a `ChatTarget` slug so the steer endpoint knows which chat thread to append injected messages to; also holds a `PendingSteerMessages` list populated when steering arrives after stdin is closed. `DrainPendingSteerMessages()` reads and atomically clears the list in one lock, preventing double-replay. `IsAwaitingUserAnswer` (bool) is set by `ClaudeStreamPump` when an `ask_user_question` event is detected and cleared by `ClaudeRunner` after the answer is received or the wait times out.
- `GigaClaw.Core/Automation/SessionRegistry.cs` — tracks active sessions per agent for steering and inactivity detection.
- `GigaClaw.Core/Automation/CostTracker.cs` — appends per-run `CostLogEntry` lines to `<workspace>/.agents/channel/cost-log.jsonl` (rotated monthly) and answers the daily-budget check (`IsBudgetExceeded`) from that file.
- `GigaClaw.Core/Automation/RunCostRecorder.cs` — hosted service subscribed to `AgentRunRegistry.OnRunEnded`. For every run that reported usage it writes the `CostLogEntry` via `CostTracker` and accumulates token/USD totals onto the ticket (`Tickets.AgentTokens` / `Tickets.AgentCostUsd` via `TicketService.AddAgentUsageAsync`) — the durable per-ticket figure shown on the board card and ticket panel, which survives the 24h run-log purge. Run-level usage also surfaces in the run drawer header and the `/runs` API DTOs.

## Entry points
- `runAgent` action from the [automation engine](./automation-engine.md).
- Ad-hoc owner prompts from the in-app new-instruction chat drawer ([Kanban UI](./kanban-ui.md)).
- `POST /api/projects/{slug}/runs/{runId}/steer` — enqueues a steering message for the active run and (when the run has a `ChatTarget`) persists it to the chat thread. Messages written while the subprocess is still running are delivered via a queued file read on the next `--resume` turn. If stdin is already closed (run has ended), `ClaudeStreamPump` calls `AgentRun.AddPendingSteerMessage` instead of silently dropping the message. On the next `POST /chat/start`, `AgentRunRegistry.LastCompletedForChatTarget()` looks up the most recent completed run for the chat target; `Endpoints.Chat.cs` calls `DrainPendingSteerMessages()` (atomic read + clear) on that run to retrieve them exactly once, carries them into `ClaudeRunContext.PendingSteerMessages`, and `ClaudeRunner.BuildPromptAsync` prepends them as steering blocks before the new user message. Additionally, when `ClaudeRunner` itself completes a chat run with `PendingSteerMessages.Count > 0`, it fires an **auto-continue** follow-up turn immediately (via `Task.Run`) using a cloned context — so the agent receives pending messages without requiring another user message. `PendingSteerMessages` is also serialised into the run snapshot so messages survive app restarts.

## External dependencies
- `claude` CLI on PATH — the actual agent runtime.
- Workspace-side `.agents/<agent>/` files (skill, memory, preamble) seeded by the [project template](./project-template.md).
- [Storage](./storage.md) — run snapshots persisted under `%APPDATA%/GigaClaw/runs/`.
